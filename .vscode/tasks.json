{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "poetry build -f wheel",
        },
        {
            "label": "publish test",
            "type": "shell",
            "command": "poetry publish --repository testpypi",
        },
        {
            "label": "publish",
            "type": "shell",
            "command": "poetry publish --repository pypi",
        }
    ]
}